CLAUDE.md
claude.md
GEMINI.md
# dependencies
/node_modules
# next.js
/.next/
/out/
# production
/build
# misc
.DS_Store
*.pem
# debug
npm-debug.log*
# typescript
*.tsbuildinfo
next-env.d.ts
# rust / tauri
/src-tauri/target
# Metrics binaries: large/third-party, produced or downloaded in CI (see release.yml),
# not versioned. The sidecar C# source and the ADLX SDK *are* committed.
/src-tauri/binaries/*.exe
.env

# Local Claude Code overrides (machine-specific permissions/hooks)
.claude/settings.local.json
# Generated audit/report artifacts kept outside the repo history
meteor-audit-*.html

# Generated by tauri-build on every compile (and rewritten even when unchanged,
# which left a permanent phantom "modified" in git status).
/src-tauri/gen/schemas
