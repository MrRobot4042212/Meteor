{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-window",
  "description": "Permisos mínimos de la ventana de ajustes in-game: solo el núcleo (invoke + eventos). No necesita diálogos, actualizador ni control de procesos, así que no los tiene: es la ventana que se abre encima de un juego.",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
